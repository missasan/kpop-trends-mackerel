name: Debug schedule

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Print debug info
        run: |
          date -u
          echo "event: $GITHUB_EVENT_NAME"
          echo "ref:   $GITHUB_REF"
